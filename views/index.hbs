<h1>User Info</h1>

{{#each usersWithServices}}
<div class="row">
	<br>
		<h4>
			<a href="/{{this.user_name}}">{{this.user_name}}</a>
		</h4>
		<br>
			<h5>
				<strong>Services:</strong>
			</h5>
			<ul>
                {{#each this.services}}
                <li>{{this.name}}</li>
                {{/each}}
			</ul>
</div>

{{/each}}